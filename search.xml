<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>RexBlog 部署设计：最小可实现方案</title>
      <link href="/2026/04/07/rexblog-deploy-design-minimal/"/>
      <url>/2026/04/07/rexblog-deploy-design-minimal/</url>
      
        <content type="html"><![CDATA[<p>这篇文章只讨论一件事：如何给 <code>RexBlog</code> 做一个足够简单、足够安全、并且后面还能继续演进的部署设计。</p><p>这里尽量不凭空设计，而是优先参考官方已经支持的静态站点部署方式。</p><p>参考来源主要是：</p><ul><li>Hexo 官方文档</li><li>GitHub Pages 官方 &#x2F; Hexo 官方 GitHub Pages 指南</li><li>Cloudflare Pages 官方 Hexo 指南</li><li>Netlify 官方仓库部署文档</li></ul><p>目标不是一开始把体系做完整，而是先让这套流程能稳定跑起来。</p><h2 id="先定义边界"><a href="#先定义边界" class="headerlink" title="先定义边界"></a>先定义边界</h2><p>这里有两个动作，必须分开：</p><ul><li><code>git push</code>：同步源码仓库</li><li><code>hexo d</code>：部署构建后的静态站点</li></ul><p>这两个动作虽然都可能发生在“发布”附近，但语义不同。</p><p><code>git push</code> 解决的是内容协作和源码同步。</p><p><code>hexo d</code> 解决的是把当前站点构建结果发布到线上。</p><p>如果这两件事混在一起，后面一旦多机协作、Actions 自动化、服务端接收端同时存在，链路就会越来越难理解。</p><h2 id="先看官方已经支持的最小路线"><a href="#先看官方已经支持的最小路线" class="headerlink" title="先看官方已经支持的最小路线"></a>先看官方已经支持的最小路线</h2><p>基于官方文档，目前最常见、也最通用的路线其实很清楚：</p><h3 id="路线一：GitHub-Pages-GitHub-Actions"><a href="#路线一：GitHub-Pages-GitHub-Actions" class="headerlink" title="路线一：GitHub Pages + GitHub Actions"></a>路线一：GitHub Pages + GitHub Actions</h3><p>这是 Hexo 官方文档直接推荐的方式之一。</p><p>基本流程：</p><ol><li>源码 push 到 GitHub 仓库默认分支</li><li>GitHub Actions 执行 <code>npm run build</code></li><li>将 <code>public/</code> 发布到 GitHub Pages</li></ol><p>优点：</p><ul><li>对 Hexo 最官方</li><li>配置简单</li><li>安全性高，不需要自建服务器</li><li>适合个人博客</li></ul><p>限制：</p><ul><li>网站托管能力受 GitHub Pages 限制</li><li>预览和扩展能力比 Cloudflare Pages 弱一些</li></ul><h3 id="路线二：Cloudflare-Pages-Git-集成"><a href="#路线二：Cloudflare-Pages-Git-集成" class="headerlink" title="路线二：Cloudflare Pages + Git 集成"></a>路线二：Cloudflare Pages + Git 集成</h3><p>Cloudflare 官方对 Hexo 有明确的 framework guide。</p><p>基本流程：</p><ol><li>把 <code>RexBlog</code> 源码推到 GitHub</li><li>Cloudflare Pages 连接 GitHub 仓库</li><li>设置：<ul><li>Production branch: <code>main</code></li><li>Build command: <code>npm run build</code></li><li>Build directory: <code>public</code></li></ul></li><li>每次提交自动构建部署</li><li>Pull Request 自动生成 preview deployment</li></ol><p>优点：</p><ul><li>配置非常少</li><li>默认就适合静态站点</li><li>PR 预览是现成能力</li><li>自定义域名和 CDN 能力很成熟</li><li>不需要自己维护服务器</li></ul><p>限制：</p><ul><li>依赖第三方平台托管</li></ul><h3 id="路线三：Netlify-Git-仓库部署"><a href="#路线三：Netlify-Git-仓库部署" class="headerlink" title="路线三：Netlify + Git 仓库部署"></a>路线三：Netlify + Git 仓库部署</h3><p>Netlify 官方也支持直接从 Git 仓库部署。</p><p>基本流程与 Cloudflare Pages 类似：</p><ol><li>连接 GitHub 仓库</li><li>配置构建命令和发布目录</li><li>平台自动构建和托管静态产物</li></ol><p>优点：</p><ul><li>同样是成熟平台方案</li><li>对静态博客非常友好</li><li>自定义域名、预览、托管能力成熟</li></ul><p>限制：</p><ul><li>对 Hexo 来说，生态心智上没有 GitHub Pages &#x2F; Cloudflare Pages 那么直接</li></ul><h2 id="现在最不该先做的事"><a href="#现在最不该先做的事" class="headerlink" title="现在最不该先做的事"></a>现在最不该先做的事</h2><p>如果目标是“快速实现 + 通用 + 安全”，现在最不该先做的是：</p><ul><li>自建部署 API</li><li>自己处理 token &#x2F; HMAC &#x2F; 重放保护</li><li>自己维护部署服务</li><li>自己维护接收端和队列</li></ul><p>这些事情都不是不能做，而是太早做会明显提高复杂度。</p><p>对于一个 Hexo 静态博客，现成托管平台已经覆盖了最关键的能力：</p><ul><li>自动构建</li><li>自动托管</li><li>自定义域名</li><li>HTTPS</li><li>PR 预览</li><li>GitHub 集成</li></ul><h2 id="最小方案"><a href="#最小方案" class="headerlink" title="最小方案"></a>最小方案</h2><p>我建议当前阶段采用最简单的版本：</p><ol><li>源码仓库使用 <code>RexBlog</code></li><li>主分支使用 <code>main</code></li><li>普通分支和 PR 只做构建校验</li><li>正式托管优先使用 GitHub Pages 或 Cloudflare Pages</li><li>不自建部署接收端</li></ol><p>这个方案里不引入额外的部署服务，不引入复杂的任务编排，也不引入一开始就必须维护的 <code>chattool serve</code> 接收端。</p><h2 id="推荐结论"><a href="#推荐结论" class="headerlink" title="推荐结论"></a>推荐结论</h2><p>如果只考虑“最安全、最通用、最快能做出来”，我建议这样选：</p><h3 id="第一推荐：Cloudflare-Pages"><a href="#第一推荐：Cloudflare-Pages" class="headerlink" title="第一推荐：Cloudflare Pages"></a>第一推荐：Cloudflare Pages</h3><p>原因：</p><ul><li>官方对 Hexo 有直接文档</li><li>接 GitHub 仓库非常直接</li><li><code>main</code> 自动发布</li><li>PR 自动预览</li><li>HTTPS、CDN、自定义域名都现成</li><li>不需要你自己管服务器</li></ul><p>如果你的重点是“把站稳定放出去，并且希望 MR &#x2F; PR 有预览”，Cloudflare Pages 是最省事的。</p><h3 id="第二推荐：GitHub-Pages"><a href="#第二推荐：GitHub-Pages" class="headerlink" title="第二推荐：GitHub Pages"></a>第二推荐：GitHub Pages</h3><p>原因：</p><ul><li>Hexo 官方文档直接支持</li><li>GitHub Actions workflow 有官方示例</li><li>对个人博客最经典</li><li>非常通用</li></ul><p>如果你更想要“全部留在 GitHub 体系里”，GitHub Pages 是最稳的默认方案。</p><h3 id="第三推荐：Netlify"><a href="#第三推荐：Netlify" class="headerlink" title="第三推荐：Netlify"></a>第三推荐：Netlify</h3><p>原因：</p><ul><li>也是成熟静态托管平台</li><li>部署方式和 Cloudflare Pages 类似</li></ul><p>但对当前这个 Hexo 博客来说，我会先优先 Cloudflare Pages 或 GitHub Pages。</p><h2 id="为什么当前不推荐自建接收端"><a href="#为什么当前不推荐自建接收端" class="headerlink" title="为什么当前不推荐自建接收端"></a>为什么当前不推荐自建接收端</h2><p>不是 <code>chattool serve</code> 不好，而是当前阶段没有必要把复杂度抬高。</p><p>如果当前目标只是让博客稳定发布，那么最短路径其实就是：</p><ul><li>源码 push 到 GitHub</li><li>平台自动构建 <code>public/</code></li><li>平台自动 host 网站</li></ul><p>这个链路有几个明显优点：</p><ul><li>官方支持</li><li>配置简单</li><li>安全边界清楚</li><li>维护成本最低</li><li>没有服务器暴露问题</li></ul><p>对于一个静态博客，这通常已经够用了。</p><h2 id="workflow-最小化"><a href="#workflow-最小化" class="headerlink" title="workflow 最小化"></a>workflow 最小化</h2><p>当前阶段 workflow 也不需要做复杂。</p><h3 id="普通分支-push"><a href="#普通分支-push" class="headerlink" title="普通分支 push"></a>普通分支 push</h3><ul><li>只做构建检查</li><li>不正式发布</li></ul><h3 id="PR-MR"><a href="#PR-MR" class="headerlink" title="PR &#x2F; MR"></a>PR &#x2F; MR</h3><ul><li>做构建检查</li><li>如果平台支持，就自动给预览地址</li></ul><h3 id="合并到-main"><a href="#合并到-main" class="headerlink" title="合并到 main"></a>合并到 <code>main</code></h3><ul><li>自动触发正式构建</li><li>自动发布到托管平台</li></ul><h2 id="本地怎么用"><a href="#本地怎么用" class="headerlink" title="本地怎么用"></a>本地怎么用</h2><p>本地继续保留简单用法：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">hexo cl</span><br><span class="line">hexo s</span><br></pre></td></tr></table></figure><p>发布不再优先依赖本地 <code>hexo d</code>，而是优先依赖 Git 平台触发：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;...&quot;</span></span><br><span class="line">git push</span><br></pre></td></tr></table></figure><p>也就是说：</p><ul><li>本地负责写内容和预览</li><li>GitHub 负责同步源码</li><li>托管平台负责构建和 host 网站</li></ul><p>这是当前最省心的方式。</p><h2 id="hexo-d-还要不要保留"><a href="#hexo-d-还要不要保留" class="headerlink" title="hexo d 还要不要保留"></a><code>hexo d</code> 还要不要保留</h2><p>可以保留，但不应该是第一优先。</p><p>原因很简单：</p><ul><li>现成托管平台已经能从源码仓库自动 build 和部署</li><li>继续依赖本地 <code>hexo d</code>，会让“本地发布”和“平台自动发布”变成两套路径</li></ul><p>如果当前目标是减少概念和减少维护，我更建议：</p><ul><li>先把正式发布统一收敛到平台自动部署</li><li><code>hexo d</code> 暂时不作为主路径</li></ul><p>后面如果你确实需要“本地一键发布构建产物”，再重新引入 <code>hexo-deployer-git</code> 也不晚。</p><h2 id="第二阶段再考虑什么"><a href="#第二阶段再考虑什么" class="headerlink" title="第二阶段再考虑什么"></a>第二阶段再考虑什么</h2><p>等下面这些需求真的出现时，再引入 <code>chattool serve</code> 更合适：</p><ul><li>不止部署博客，还部署多个模型生成站点</li><li>需要统一接入部署日志和回滚</li><li>需要统一鉴权和部署队列</li><li>需要服务端接收层，而不是完全依赖第三方托管平台</li></ul><p>那时 <code>chattool serve</code> 才真正有必要。</p><h2 id="当前推荐的最终方案"><a href="#当前推荐的最终方案" class="headerlink" title="当前推荐的最终方案"></a>当前推荐的最终方案</h2><p>如果只考虑“现在就能做、最安全、最通用、最少设计”，我推荐：</p><ol><li>源码仓库继续放在 GitHub</li><li>正式托管优先选 Cloudflare Pages 或 GitHub Pages</li><li>普通分支和 PR 只做构建检查</li><li><code>main</code> 负责正式发布</li><li>暂不自建 <code>chattool serve</code> 接收端</li></ol><p>如果你问我只选一个，我会推荐：</p><ol><li>需要 PR 预览和更强托管体验：<code>Cloudflare Pages</code></li><li>需要最经典、最官方、最少心智负担：<code>GitHub Pages</code></li></ol><p>这就是当前阶段最小、最安全、最通用的方案。</p>]]></content>
      
      
      
        <tags>
            
            <tag> deployment </tag>
            
            <tag> workflow </tag>
            
            <tag> design </tag>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>AI 试炼场的多机协作与部署思路</title>
      <link href="/2026/04/07/model-output-deploy-workflow/"/>
      <url>/2026/04/07/model-output-deploy-workflow/</url>
      
        <content type="html"><![CDATA[<p>AI 试炼场后面会持续记录模型生成的网站、文章、实验过程和整理结果，因此内容来源不会只来自一台机器，也不会只来自一个人手工编辑。</p><p>比较自然的演进方式是：多台机器各自产出内容，通过 GitHub 汇总，再由一条受控的发布链路完成部署。</p><h2 id="目标"><a href="#目标" class="headerlink" title="目标"></a>目标</h2><ul><li>让多台机器都能产出内容并同步到同一个博客仓库。</li><li>保持服务器不直接暴露给每一台机器。</li><li>让部署入口尽量单一，避免状态漂移。</li><li>保留审查和回滚能力。</li></ul><h2 id="多机协作分支模型"><a href="#多机协作分支模型" class="headerlink" title="多机协作分支模型"></a>多机协作分支模型</h2><p>一个比较稳的方式是给每台机器一个长期分支，例如：</p><ul><li><code>machine/macbook</code></li><li><code>machine/workstation</code></li><li><code>machine/server-lab</code></li></ul><p>每台机器上的模型产出、草稿整理、配置试验，都先落在自己的分支上。这样做的好处是：</p><ul><li>机器之间不会直接相互覆盖。</li><li>容易追踪某一类内容是在哪台机器上产生的。</li><li>某台机器的试验性改动不会直接污染发布分支。</li></ul><p>真正对外发布时，只允许 <code>main</code> 作为发布源。这样 GitHub 就是唯一的汇总点，服务器不需要面对来自多台机器的直接写入。</p><p>如果后面内容量变大，还可以再加一层约束：</p><ul><li><code>main</code> 只放已经确认可发布的内容。</li><li><code>machine/*</code> 只做同步和草稿。</li><li>需要发布时，通过 PR 或手动 merge 进入 <code>main</code>。</li></ul><h2 id="方案一：每台机器直连服务器部署"><a href="#方案一：每台机器直连服务器部署" class="headerlink" title="方案一：每台机器直连服务器部署"></a>方案一：每台机器直连服务器部署</h2><p>这是最直接的方式。每台机器生成静态文件后，直接推到服务器，或者直接 SSH 到服务器执行更新。</p><p>优点：</p><ul><li>路径最短。</li><li>排查问题时比较直接。</li><li>不依赖 GitHub Actions。</li></ul><p>缺点：</p><ul><li>每台机器都要持有服务器凭据。</li><li>服务器暴露面会随着机器数量增长而扩大。</li><li>很难保证发布入口一致。</li><li>一旦某台机器状态脏了，可能直接把问题带到线上。</li></ul><p>如果这是一个长期运行、多人或多机都可能参与产出的博客，这个方案不适合作为默认方案。</p><h2 id="方案二：GitHub-Actions-自动部署"><a href="#方案二：GitHub-Actions-自动部署" class="headerlink" title="方案二：GitHub Actions 自动部署"></a>方案二：GitHub Actions 自动部署</h2><p>这是静态博客最自然的方案之一。</p><p>基本链路可以是：</p><ol><li>每台机器把内容推到自己的分支。</li><li>需要发布的内容合并到 <code>main</code>。</li><li>GitHub Actions 在 <code>main</code> 更新后执行构建。</li><li>构建产物自动部署到目标环境。</li></ol><p>部署目标可以有几种：</p><ul><li>GitHub Pages</li><li>服务器上的静态目录</li><li>对象存储 + CDN</li></ul><p>优点：</p><ul><li>所有机器都不需要直接接触服务器。</li><li>发布入口统一，审计记录清晰。</li><li>很适合静态站点。</li><li>回滚、重跑、对比构建结果都比较方便。</li></ul><p>缺点：</p><ul><li>需要维护 CI 配置。</li><li>如果部署到自建服务器，仍然需要在 GitHub Secrets 中保存某种凭据。</li><li>某些复杂的服务端逻辑不如自建发布服务灵活。</li></ul><p>如果 AI 试炼场本质上还是一个 Hexo 静态博客，这个方案通常是默认推荐。</p><h2 id="方案三：自建推送服务"><a href="#方案三：自建推送服务" class="headerlink" title="方案三：自建推送服务"></a>方案三：自建推送服务</h2><p>你提到的方式是服务器上跑一个 <code>chattool serve</code> 风格的服务，通过鉴权 token 接收发布请求，再触发仓库更新、构建和切换。</p><p>这个方案的核心思路其实不是让每台机器去碰服务器，而是让服务器只暴露一个受控的发布入口。</p><p>优点：</p><ul><li>可以把部署逻辑全部集中到服务端。</li><li>可以做更细的鉴权、审计、限流和灰度。</li><li>可以扩展成“接收 GitHub Webhook 后自动拉取部署”。</li><li>后面如果不仅是博客，还想部署别的模型产出网站，也比较容易复用。</li></ul><p>缺点：</p><ul><li>需要自己维护服务的安全性和稳定性。</li><li>需要处理 token、重放防护、日志、失败重试等细节。</li><li>系统复杂度高于纯 GitHub Actions。</li></ul><p>这个方案更适合作为第二阶段，而不是第一阶段。</p><h2 id="更推荐的组合方式"><a href="#更推荐的组合方式" class="headerlink" title="更推荐的组合方式"></a>更推荐的组合方式</h2><p>如果要兼顾安全、简单和可扩展，我更推荐下面这个组合：</p><ol><li>多台机器各自使用 <code>machine/*</code> 分支同步内容。</li><li><code>main</code> 作为唯一发布分支。</li><li>GitHub 作为唯一协作和汇总入口。</li><li>部署只从 <code>main</code> 触发。</li></ol><p>在“部署只从 <code>main</code> 触发”这个前提下，可以再分成两种落地方式：</p><h3 id="组合-A：GitHub-Actions-直接部署"><a href="#组合-A：GitHub-Actions-直接部署" class="headerlink" title="组合 A：GitHub Actions 直接部署"></a>组合 A：GitHub Actions 直接部署</h3><p>适合场景：</p><ul><li>你希望先快速稳定地跑起来。</li><li>站点本质就是静态文件。</li><li>服务器侧不想额外维护一个发布服务。</li></ul><p>推荐度：高。</p><h3 id="组合-B：GitHub-Actions-只负责触发，自建服务负责部署"><a href="#组合-B：GitHub-Actions-只负责触发，自建服务负责部署" class="headerlink" title="组合 B：GitHub Actions 只负责触发，自建服务负责部署"></a>组合 B：GitHub Actions 只负责触发，自建服务负责部署</h3><p>链路是：</p><ol><li><code>main</code> 更新。</li><li>GitHub Actions 通过受控 token 调用服务器上的部署接口。</li><li>服务器自己 <code>git pull</code>、<code>npm ci</code>、<code>hexo generate</code>、切换静态目录。</li></ol><p>这样做比“每台机器直连服务器”安全很多，因为真正接触服务器的只有 GitHub Actions 和服务器本身。</p><p>推荐度：中高。</p><p>如果以后 AI 试炼场不仅发布博客，还会发布多个模型生成站点，这个方向会更有扩展性。</p><h2 id="不建议的形态"><a href="#不建议的形态" class="headerlink" title="不建议的形态"></a>不建议的形态</h2><p>不太建议让每台机器都各自持有服务器部署权限，然后从本地直接上线。</p><p>问题不只是安全，还包括：</p><ul><li>发布行为不可控。</li><li>很难知道线上版本到底来自哪台机器。</li><li>很难复现和回滚。</li><li>一旦某台机器构建环境不同，线上结果可能就不同。</li></ul><h2 id="当前阶段的建议"><a href="#当前阶段的建议" class="headerlink" title="当前阶段的建议"></a>当前阶段的建议</h2><p>如果现在的重点是先把 AI 试炼场稳定运行起来，我会建议：</p><ol><li>先采用“多机分支 + GitHub 汇总 + <code>main</code> 发布”的模型。</li><li>第一阶段优先使用 GitHub Actions 做构建校验，并由 Actions 调用服务器上的 deploy hook。</li><li>等后面模型生成的网站种类更多、部署链路更复杂时，再把 deploy hook 扩展成统一发布服务。</li></ol><p>换句话说：</p><ul><li>GitHub 负责协作和汇总。</li><li><code>main</code> 负责发布语义。</li><li>Actions 负责构建校验和受控触发。</li><li>服务器 deploy hook 负责最终部署和回滚控制。</li></ul><h2 id="本站准备采用的落地方式"><a href="#本站准备采用的落地方式" class="headerlink" title="本站准备采用的落地方式"></a>本站准备采用的落地方式</h2><p>AI 试炼场当前准备采用更接近传统 Hexo 和 <code>butterfly</code> 旧站的方式：</p><ol><li>多台机器各自使用 <code>machine/*</code> 分支同步源码内容。</li><li>需要上线的内容合并到 <code>main</code>。</li><li><code>git push</code> 负责同步源码仓库。</li><li><code>hexo d</code> 负责部署构建后的静态站点。</li><li><code>hexo d</code> 使用标准 <code>hexo-deployer-git</code> 将 <code>public/</code> 推送到部署目标。</li><li>GitHub Actions 也复用同一条 <code>hexo d</code> workflow。</li></ol><p>这样做的关键点是：</p><ul><li>源码同步和网站部署是两回事。</li><li>部署对象是构建产物，不是源码仓库。</li><li>本地和 CI 走的是同一套部署语义。</li><li>部署更新天然保留 Git 差量特性。</li></ul><p>如果后续需要更强的服务端统一控制，再把 deploy hook 作为第二阶段扩展即可。当前默认方案优先保持简单。</p><h2 id="后续可以继续细化的点"><a href="#后续可以继续细化的点" class="headerlink" title="后续可以继续细化的点"></a>后续可以继续细化的点</h2><ul><li>分支命名规范</li><li>PR 合并规范</li><li>自动构建和预览环境</li><li>服务器上的目录切换与回滚策略</li><li>文章与模型产出的目录组织方式</li></ul><p>这套机制的核心，不是“怎么把文件传上去”，而是“怎么让模型产出在多机协作下仍然可控、可审计、可回滚”。</p>]]></content>
      
      
      
        <tags>
            
            <tag> ai </tag>
            
            <tag> deployment </tag>
            
            <tag> github </tag>
            
            <tag> workflow </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Hello World</title>
      <link href="/2026/04/06/hello-world/"/>
      <url>/2026/04/06/hello-world/</url>
      
        <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues">GitHub</a>.</p><h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/writing.html">Writing</a></p><h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo server</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/server.html">Server</a></p><h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo generate</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/generating.html">Generating</a></p><h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">$ hexo deploy</span><br></pre></td></tr></table></figure><p>More info: <a href="https://hexo.io/docs/one-command-deployment.html">Deployment</a></p>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
